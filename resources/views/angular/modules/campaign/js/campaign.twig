'use strict';
//APIService
app.controller('CampaignCtrl', ['$scope', '$state', '$http',
                        function( $scope,   $state,   $http) {
    
    $scope.varSelect = '123';

    $scope.onPage = 3;

    $scope.list = {itemsByPage:10, totalItems:211}

    $scope.list.header      = {};
    $scope.list.table       = {};
    $scope.list.items       = [];
    
    //Header Config
    $scope.list.header.title = "Campaigns";

    //Table Head Config
    $scope.list.table.head = [];
    $scope.list.table.head.push({key:'id',name:'ID'});
    $scope.list.table.head.push({key:'name',name:'Name'});
    $scope.list.table.head.push({key:'budget_daily',name:'B. Daily'});
    $scope.list.table.head.push({key:'budget_total',name:'B. Total'});

    //Items Config
    $scope.list.items.push({id:1,name:'Name1',budget_daily:10,budget_total:50});
    $scope.list.items.push({id:2,name:'Name2',budget_daily:11,budget_total:51});

    //Paginator Config
    $scope.list.table.paginator = {};
    $scope.list.table.paginator.pagesByRotatore = 5;
    $scope.list.table.paginator.onPage = 1;

    $scope.changeValue = function(){

        //alert($scope.varSelect);
        //console.log($scope.varSelect);
        //$scope.varSelect = $scope.varSelect;
    }

    $scope.list.table.paginator.changePage = function(){
        console.log('Pagina: '+$scope.list.onPage);
    }

    
    $scope.init = function(){
       
    }

    $scope.init();
        
}]);