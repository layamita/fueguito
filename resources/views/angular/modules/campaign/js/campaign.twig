'use strict';

app.controller('CampaignCtrl', ['$scope', '$state', '$http', 'APIService',
                        function( $scope,   $state,   $http, APIService) {
    
    
    
    
    $scope.init = function(){
        
    }

    $scope.init();
        
}]);